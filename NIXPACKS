[phases.setup]
  nixPkgs = ['nodejs-16.20.2']

[phases.build]
  cmds = [
    "npm install",
    "npm run build"
  ]

[phases.start]
  cmd = "npm start"

[phases.install]
  cmds = [
    "npm install -g npm@8.19.4"
  ]
